name: Render dashboard

# 时间是 UTC。北京时间 = UTC+8。
# "*/15 22-23,0-16 * * *" 对应北京 06:00–00:59 每 15 分钟一次。
# 凌晨不跑 —— 那几个小时没人看。
#
# 本仓库是公开的（里面只有代码和字体，零个人信息），所以 Actions 免费无限，
# 不必为额度压缩频率。课表和待办这些内容渲染后推到另一个私有仓库，不公开。
on:
  schedule:
    - cron: "*/15 22-23,0-16 * * *"
  push:
    paths:
      - "render/**"
      - "data/**"
      - "assets/**"
      - ".github/workflows/render.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: render-dashboard
  cancel-in-progress: true    # 排队的旧任务没有意义，直接取消

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 6

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: render/requirements.txt

      - name: 安装依赖
        run: pip install -r render/requirements.txt

      - name: 渲染
        env:
          LARK_APP_ID: ${{ secrets.LARK_APP_ID }}
          LARK_APP_SECRET: ${{ secrets.LARK_APP_SECRET }}
          # app_token 也走 Secret —— 本仓库是公开的，配置文件里留空
          LARK_APP_TOKEN: ${{ secrets.LARK_APP_TOKEN }}
        run: python render/render.py --out out

      - name: 校验设备绘制区留白
        # 四块区域都由设备实时重绘，图里必须是白的。有内容的话，设备的 -k
        # 会把它擦掉（留下缺角），或者两者叠在一起 —— 时钟那次就是叠出了
        # 两个时间。调版面最容易在这里出错，所以逐块校验。
        # 这段与 deploy.ps1 里的校验是同一套逻辑，本地和 CI 都拦得住。
        run: |
          python - <<'PY'
          import json, sys
          from PIL import Image
          hm = json.load(open('out/hitmap.json', encoding='utf-8'))
          img = Image.open('out/dash.png')
          bad = 0
          for key in ('clock', 'next', 'status', 'sync'):
              c = hm[key]['cls']
              x1, y1 = c['left'], c['top'] - hm['top_offset']
              dark = sum(img.crop(
                  (x1, y1, x1 + c['width'], y1 + c['height'])).histogram()[:128])
              print(f'{key:7s} 暗像素 = {dark}' + ('  <<< 有内容' if dark > 200 else ''))
              bad += dark > 200
          sys.exit(1 if bad else 0)
          PY

      - name: 推送到私有输出仓库
        env:
          OUT_TOKEN: ${{ secrets.OUT_REPO_TOKEN }}
          OUT_REPO: ${{ secrets.OUT_REPO }}
        run: |
          set -euo pipefail
          if [ -z "${OUT_TOKEN:-}" ] || [ -z "${OUT_REPO:-}" ]; then
            echo "未配置 OUT_REPO / OUT_REPO_TOKEN，跳过推送"
            exit 0
          fi

          REMOTE="https://x-access-token:${OUT_TOKEN}@github.com/${OUT_REPO}.git"

          # 先比对内容：没变就不推。
          #
          # 比对时必须排除 device.conf 里的 GENERATED_AT —— 它每次渲染都变，
          # 不排除的话这个判断永远为真，输出仓库每 15 分钟多一个 commit，
          # 设备也会每个周期做一次无谓的整屏刷新（墨水屏上肉眼可见）。
          # 同理，渲染出的图里也刻意不含任何时间戳。
          sig() { cat "$1"; grep -v '^GENERATED_AT=' "$2"; }
          NEW=$(sig out/dash.png out/device.conf | sha256sum | cut -d' ' -f1)
          SRC=$PWD
          OLD=none

          # 比对必须在 checkout 目录【之外】进行。
          #
          # actions/checkout 会往本仓库的 git 配置里注入
          # http.https://github.com/.extraheader，内容是它自己 GITHUB_TOKEN 的
          # Authorization 头。在 checkout 目录里执行 git 时，这个 header 会
          # 覆盖 URL 里带的凭据 —— 而 GITHUB_TOKEN 对输出仓库没有任何权限，
          # 于是鉴权失败被报成「分支不存在」，比对逻辑永远走不到。
          # （后面的 push 之所以正常，正是因为它已经 cd 到了临时目录。）
          TMP0=$(mktemp -d)
          cd "$TMP0"
          if git clone -q --depth 1 "$REMOTE" prev 2>/dev/null; then
            if [ -f prev/dash.png ] && [ -f prev/device.conf ]; then
              OLD=$(sig prev/dash.png prev/device.conf | sha256sum | cut -d' ' -f1)
              echo "  图   新=$(sha256sum "$SRC/out/dash.png" | cut -c1-12) 旧=$(sha256sum prev/dash.png | cut -c1-12)"
              echo "  配置 新=$(grep -v '^GENERATED_AT=' "$SRC/out/device.conf" | sha256sum | cut -c1-12) 旧=$(grep -v '^GENERATED_AT=' prev/device.conf | sha256sum | cut -c1-12)"
            else
              echo "远端缺少产物文件，视为首次推送"
            fi
          else
            echo "远端仓库为空或无法取回，视为首次推送"
          fi
          cd "$SRC"
          echo "签名  新=${NEW:0:16}  旧=${OLD:0:16}"
          if [ "$NEW" = "$OLD" ]; then
            echo "内容未变，跳过推送"
            exit 0
          fi

          # 用一次性工作目录重建单 commit 历史再强推 ——
          # 输出仓库永远只有一个 commit，不会随时间膨胀。
          TMP=$(mktemp -d)
          cp out/dash.png out/device.conf out/device-font.ttf out/hitmap.json "$TMP/"
          cd "$TMP"
          git init -q
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -q -m "render $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push -q --force "$REMOTE" HEAD:main
          echo "已推送到 $OUT_REPO"
