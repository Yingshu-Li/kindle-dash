Pillow>=10.0
PyYAML>=6.0
requests>=2.31
# Windows 没有系统级 IANA 时区库，zoneinfo 依赖此包；Linux 上冗余但无害
tzdata>=2024.1
# 用于按实际课表生成设备端字体子集（固化字重 + 只保留用到的字形）。
# 本地开发时是手动装的，漏写在这里导致 CI 首次运行就挂了。
fonttools>=4.50
brotli>=1.1
